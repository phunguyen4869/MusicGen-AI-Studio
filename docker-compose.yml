version: '3.8'

services:
  musicgen-studio:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: musicgen-studio-app
    ports:
      - "8501:8501"
    env_file:
      - .env
    restart: unless-stopped
